""" Use to run the scheduler and other bash command """ 

import os 
from ..constants import ffmpeg_const 

class Kommand: 
  path = Config.
  
  @classmethod 
  def create_dir(cls,name): 
    dir_name = "/".join()
    os.makedirs()

